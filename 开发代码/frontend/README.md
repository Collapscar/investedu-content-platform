
# 金融投教内容资产平台前端

基于 Figma 导出代码迁移，技术栈为 Vite + React + TypeScript + Tailwind。

## Running the code

Run `npm install` to install the dependencies.

Run `npm run dev` to start the development server.

本地开发时 `/api` 会代理到 `http://localhost:8080`，需要先启动 `../backend`。
